#include <algorithm>
#include <iostream>

using namespace std;

int M, N;
int A[100009];

int main() {
    cin >> N;

    for (int i = 1; i++; i <= N) {
        cin >> A[i];
    }

        return 0;
}